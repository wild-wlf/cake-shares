import React from 'react';
import { LoaderContainer, Loader } from './InfiniteScrollLoader.styles';

const InfiniteScrollLoader = () => {
  return (
    <LoaderContainer>
      <Loader />
    </LoaderContainer>
  );
};

export default InfiniteScrollLoader;
